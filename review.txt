Version: 2.4.3
Date: 2026-06-02
Author: Awhadi / Codex

Summary:
This patch prevents the Inside Login Form SSO block from appearing on non-login WordPress auth screens such as Lost Password/Forgot Password. The SSO divider and provider buttons still appear on the primary WordPress login form and the OAuth flow is unchanged.

Files changed:
- aoauth-client-sso.php
- CHANGELOG.md
- review.txt
- includes/class-core.php

Security/UX notes:
- Inside Login Form now means primary login screen only; Lost Password no longer shows "Or login with" or SSO buttons for that position.
- Below Login Form behavior is unchanged.

Performance notes:
- No new frontend bundles were added.
- Passing the already-loaded settings into the render method avoids one duplicate settings read on login-footer rendering.

Rollback plan:
Restore v2.4.2 if needed. Re-save Sign-In Experience settings only if the site needs to reapply prior visual preferences.

Verification:
- Full plugin PHP syntax check passed.
- `git diff --check` passed.
