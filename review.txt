Version: 2.4.2
Date: 2026-06-02
Author: Awhadi / Codex

Summary:
This patch tightens the WordPress login SSO layout, fixes an account-linking lockout variable bug, and makes configured bot verification overlay opacity apply consistently to the supported selected overlay styles. Provider configuration, OAuth callbacks, and bot verification token validation behavior are unchanged.

Files changed:
- aoauth-client-sso.php
- CHANGELOG.md
- review.txt
- includes/class-security.php
- public/css/login-single-sign-on.css

Security/UX notes:
- Inside Login Form now means username/password, Remember Me, Login button, then "Or login with" and SSO buttons.
- Full Login Ban now has the configured duration available when repeated failed account-linking password checks trigger a lockout.
- Overlay opacity now controls Spotlight animation end state and Minimal overlay opacity instead of being overwritten by fixed CSS values.

Performance notes:
- No new frontend bundles were added.
- Existing public login CSS was reused for layout and overlay changes.
- The PHP fix adds no database work or new runtime dependencies.

Rollback plan:
Restore v2.4.1 if needed. Re-save Security and Sign-In Experience settings only if the site needs to reapply prior visual preferences.

Verification:
- Full plugin PHP syntax check passed.
- JavaScript syntax check passed for public/js/login-single-sign-on.js.
- `git diff --check` passed.
